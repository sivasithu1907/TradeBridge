# Leave unset for local dev -- the Vite dev server proxies /api to the
# backend automatically (see vite.config.ts). Only set this if your
# frontend and backend are on different domains/ports without a proxy.
# VITE_API_URL=http://localhost:4000/api
